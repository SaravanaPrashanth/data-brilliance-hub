import type { NextConfig } from "next";

const nextConfig: NextConfig = {
    /* config options here */
    experimental: {
        // Turbopack is default, but we can ensure experimental features if needed
    },
};

export default nextConfig;
